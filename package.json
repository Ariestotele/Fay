{
  "name": "fay",
  "version": "0.2.2",
  "private": true,
  "description": "Fay — a minimal dark desktop launcher hub for Windows.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
